
import { ButtonContainer } from './styles';

const Button = ({label, onCLick}) => {
    return (
      <ButtonContainer onCl>
        1
      </ButtonContainer>
    );
  }
  
  export default Button;