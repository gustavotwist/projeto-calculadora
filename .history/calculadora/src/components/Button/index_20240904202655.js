
import { ButtonContainer } from './styles';

const Button = ({label, onCLick}) => {
    return (
      <ButtonContainer onClick={on}>
        1
      </ButtonContainer>
    );
  }
  
  export default Button;