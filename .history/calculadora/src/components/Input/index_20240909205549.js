import { InputContainer } from './styles';

const Input = (value) => {
    return (
      <InputContainer>
        <input disabled></input>
      </InputContainer>
    );
  }
  
  export default Input;